{
  "scripts": {
    "dist" : "mkdir -p deliverables && elm make --output=deliverables/main.js src/Main.elm && cp src/index.html deliverables"
  },
  "devDependencies": {
    "elm": "^0.19.1-5"
  },
  "dependencies": {
    "elm-live": "^4.0.2"
  }
}
